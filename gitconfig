[user]
	name = Stefan Eletzhofer
	email = se@nexiles.de
[color]
	status = auto
	diff = auto
	branch = auto
	interactive = auto
        ui = true
[alias]
	st = status -s
	stu = status -suno
	ci = commit
	co = checkout
	d = icdiff
	dc = diff --cached
	l  = log --graph --pretty=format':%C(yellow)%h%Cblue%d%Creset %s %C(white) %an, %ar%Creset'
        lol = log --pretty=oneline --abbrev-commit --graph --decorate
[merge]
	tool = opendiff
[core]
	excludesfile = /Users/seletz/.gitignore
	whitespace = fix
[github]
	user = seletz
[ghi]
	token = !security find-internet-password -a seletz -s github.com -l 'ghi token' -w
[push]
	default = tracking
[filter "media"]
	clean = git-media-clean %f
	smudge = git-media-smudge %f
